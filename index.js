// Addition
const sum = function add(a, b) {
    return a + b;
}

const total = sum(8, 9);
console.log(total);

// Division
const sol = function divide(a, b) {
    return a / b;
}

const res = sol(9, 3);
console.log(res);