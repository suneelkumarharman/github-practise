// Multiplication

var mul=function mul (a, b) {
       return a+b;
   }
   
   const total =mul(3,5);
  

//Subtraction

var sub = function sub (a, b) {
       return a-b;
   }
   
   const res =sub(3,5);
   
